<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <%= stylesheet_link_tag 'scaffold', 'application', 'simple_form' %>
    <%= javascript_include_tag :defaults %>
    <%= csrf_meta_tag %>
    <%= render 'shared/ga' %>
  </head>
  <body>
    <div id="container">
      <header id="page_header">
        <h1>Блог 7even</h1>
        <h2>про ruby, rails, sinatra, git и всё на свете</h2>
        
        <nav>
          <ul>
            <li><%= link_to_unless_current 'Последние записи', root_path %></li>
            <li><%= link_to 'Архив', '/archive' %></li>
            <li><%= link_to 'Контакты', '/contacts' %></li>
            <li><%= link_to 'Обо мне', '/about' %></li>
          </ul>
          
          <%= render 'shared/auth' %>
        </nav>
      </header>
      
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <% end %>
      
      <%= yield %>
      
      <footer id="page_footer">
        <p>
          <span>navigator.userAgent.should_not =~ /IE/</span><br />
          &copy; 2011 <%= mail_to '7@7vn.ru', '7even', encode: 'javascript' %>
        </p>
      </footer>
    </div>
  </body>
</html>
